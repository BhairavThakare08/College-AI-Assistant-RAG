# LangGraph / LangChain core
langgraph>=1.0.0
langchain>=1.3.0
langchain-groq>=1.1.0
langchain-community>=0.3.0
langchain-huggingface>=1.2.0
langchain-text-splitters>=0.3.0

# Embeddings + vector store
# Embeddings run via Hugging Face's hosted Inference API (HuggingFaceEndpointEmbeddings)
# instead of loading the model locally — no sentence-transformers/torch needed,
# which keeps this well under Render's free-tier 512MB RAM limit.
faiss-cpu>=1.8.0
pypdf>=5.0.0

# Backend API
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic>=2.10.0

# Utilities
python-dotenv>=1.0.0
